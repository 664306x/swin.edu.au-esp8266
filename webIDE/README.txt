Version 1.0

All you have to do is change the SSID and PASSWORD in webIDE.lua

Upload all the files then hard restart it.

Output file will continuity get bigger and may serve as a memory issue (run file.remove("output.lua") to solve)

If you refresh and nothing that was previouly printed except for WebIDE start it probably crashed/restarted